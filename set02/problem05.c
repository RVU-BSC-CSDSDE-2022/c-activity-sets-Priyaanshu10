#include <stdio.h>
#include <math>

int input()
{
  int n;
  printf("Enter the number");
  scanf("%f",&n);
  return n;
}

float borga_X(int n)
{
int a=0;
  x=pow(n,a)
  for(q=2;q++)
  z=x/a+q
  for(z<0;a++)
  
}